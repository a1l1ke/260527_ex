# AGENTS.md

## 에이전트 행동 지침 (Agent Behavior Guidelines)

### 응답 품질

- **모든 응답은 최소 100자 이상** 이어야 합니다.  
  단순한 단답 대신, 작업의 맥락·이유·영향을 함께 설명하여 사용자가 결과를 충분히 이해할 수 있도록 합니다.
- **친절하고 명확한 어조**를 유지합니다.  
  전문 용어를 사용할 때는 간략한 설명을 덧붙이고, 사용자 입장에서 이해하기 쉬운 표현을 우선합니다.
- **상세한 설명을 제공**합니다.  
  변경 사항이 있을 경우 무엇을(What), 왜(Why), 어떻게(How) 변경했는지를 명시합니다.  
  코드 수정 시에는 수정 전후 차이와 의도를 함께 기술합니다.

### 계획 및 실행

- 복잡한 작업은 실행 전에 계획을 먼저 제시하고, 사용자 승인 후 진행합니다.
- 파일을 수정하기 전에 기존 내용을 파악하고, 불필요한 덮어쓰기를 방지합니다.
- 작업 완료 후에는 변경된 내용을 요약하여 보고합니다.

### 코드 스타일

- 기존 코드의 스타일과 컨벤션을 따릅니다.
- 새 파일 생성 시 프로젝트의 디렉터리 구조와 네이밍 규칙을 준수합니다.
- HTML/CSS/JS 파일은 가독성을 위해 적절한 들여쓰기와 주석을 포함합니다.
- 프런트엔드 디자인은 DESIGN.md를 참고하여 진행할 것

---

## 스킬 (Skills)

이 프로젝트에는 재사용 가능한 에이전트 스킬이 정의되어 있습니다.  
스킬 파일 위치: `.agent/skills/<skill-name>/SKILL.md`

### git-commit 스킬

**트리거 키워드**: 커밋, commit, 푸시, push, 깃 커밋, 변경사항 저장, 올려줘, 반영해줘

사용자가 위 키워드를 언급하거나 변경 내용을 저장·공유하려는 의도가 감지되면 이 스킬을 로드합니다:
`.agent/skills/git-commit/SKILL.md`

**핵심 규칙**:
- 커밋 메시지는 반드시 `<영어 타입>: <한글 메시지>` 형식 (예: `feat: 로그인 페이지 추가`)
- 변경 파일을 기능·유형 단위로 **잘게 분리**하여 원자적 커밋 생성
- 모든 커밋 완료 후 `git push`로 원격에 반영
- `git push --force` 및 `git add .` 일괄 커밋은 금지
